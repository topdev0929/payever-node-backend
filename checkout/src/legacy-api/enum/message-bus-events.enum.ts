export enum MessageBusEventsEnum {
  actionApiCallCreated = 'checkout.event.action-api-call.created',
  actionApiCallMigrate = 'checkout.event.action-api-call.migrate',

  orderCreated = 'checkout.event.order.created',
}
