export enum CheckoutEvent {
  CheckoutCreated = 'checkout.created',
  CheckoutUpdated = 'checkout.updated',
  CheckoutRemoved = 'checkout.removed',

  CheckAndCreateDefaultCheckout = 'check.and.create.default.checkout',
}

