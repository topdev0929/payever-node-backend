export enum ContactRabbitEventsEnum {
  ContactCreated = 'contacts.event.contact.created',
  ContactUpdated = 'contacts.event.contact.updated',
  ContactRemoved = 'contacts.event.contact.removed',
  ContactExported = 'contacts.event.contact.exported',
}
