export enum AppointmentRabbitEventsEnum {
  AppointmentCreated = 'appointments.event.appointment.created',
  AppointmentUpdated = 'appointments.event.appointment.updated',
  AppointmentRemoved = 'appointments.event.appointment.removed',
  AppointmentExport = 'appointments.event.appointment.export',
}
