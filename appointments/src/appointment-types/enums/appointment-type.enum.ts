export enum AppointmentSchedulesEnum {
  WorkingHours = 'working_hours',
}

export enum AppointmentTypesEnum {
  OneOnOne = 'one_on_one',
  Group = 'group',
  Now = 'now',
}

export enum AppointmentDurationUnitsEnum {
  Hour = 'hour',
  Min = 'min',
}
