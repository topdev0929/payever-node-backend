export enum PlanEventsEnum {
  PlanDeleted = 'plan.deleted',
  PlanCreated = 'plan.created',
  PlanUpdated = 'plan.updated',
  PlanSubscribe = 'plan.subscribe',
  PlanUnsubscribe = 'plan.unsubscribe',
}
