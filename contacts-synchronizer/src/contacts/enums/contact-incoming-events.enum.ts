export enum ContactsIncomingSynchronizationEventsEnum {
  Created   = 'contacts.event.contact-synchronization.created',
  Updated   = 'contacts.event.contact-synchronization.updated',
  Failed    = 'contacts.event.contact-synchronization.failed',
}

export enum ContactsIncomingEventsEnum {
  Created = 'contacts.event.contact.created',
  Updated = 'contacts.event.contact.updated',
  Removed = 'contacts.event.contact.removed',
}
