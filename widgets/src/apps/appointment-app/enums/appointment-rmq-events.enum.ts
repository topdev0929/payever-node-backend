export enum AppointmentRabbitEventsEnum {
  created = 'appointments.event.appointment.created',
  updated = 'appointments.event.appointment.updated',
  removed = 'appointments.event.appointment.removed',
  export = 'appointments.event.appointment.export',
}
